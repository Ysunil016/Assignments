package com.Spring.Services.Interface;

import com.Spring.Bean.Booking;
import com.Spring.Bean.UserRecord;
/**
 * 
 * @author sunil016, Interface holds all the functions related to Booking Service
 *
 */

public interface BookingInterface {

	boolean makeASlotBooking(Booking book, String from);

	Object getAllBookings(String username);

	boolean isUserValid(String username);

	boolean removeSlotFromUserRecord(UserRecord userRecord, Integer slot);

	boolean saveUserRecord(UserRecord userRecord);

	boolean saveBooking(Booking bookRecord);
}
